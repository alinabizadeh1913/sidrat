export type AuraSettings = {
    particleCount: number;
    particleColor: string;
    minSpeed: number;
    maxSpeed: number;
    minSize: number;
    maxSize: number;
    glowIntensity: number;
  };
  